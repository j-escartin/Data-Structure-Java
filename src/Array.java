/**
 * created by James 6/30/23
 **/
public class Array {

    /*
    Arrays is the most simple data structure

    Properties
      1. each element the same data type
      2. Elements are stored at contiguous memory location which is the
        first element is stored in the smallest memory location
      3. Each element can be randomly accessed

      zero-based indexing

      */

    //declaring with values
    int arr[] = {1,2,3,4,5};

    //declaring for with specified spaces
    int[] arrayWithSpace = new int[5];
}
